# Tạo file kịch bản báo cáo chi tiết

script_content = """# KỊCH BẢN BÁO CÁO ĐỒ ÁN TỐT NGHIỆP
# LEXIVOCAB - HỆ SINH THÁI HỌC TỪ VỰNG OMNICHANNEL
# Sinh viên: Phan Văn Thành | Mã SV: DTH225766

## ⏱ PHÂN BỔ THỜI GIAN
- **Phần Thuyết trình:** 15 phút
- **Phần Demo:** 10 phút
- **Phần Hỏi đáp:** 5 phút
- **Tổng:** 30 phút

---

## PHẦN 1: MỞ ĐẦU & ĐẶT VẤN ĐỀ (2 PHÚT)

### SLIDE 1: Trang bìa
**Nội dung slide:**
- Tên đề tài: LexiVocab - Nền tảng học từ vựng chủ động đa môi trường
- Sinh viên: Phan Văn Thành
- Mã sinh viên: DTH225766
- Giảng viên hướng dẫn
- Logo/Khẩu hiệu: "Capture. Remember. Understand."

**Lời thoại:**
"Kính chào quý thầy cô trong Hội đồng. Em là Phan Văn Thành, mã sinh viên DTH225766. Hôm nay em rất vinh dự được trình bày đồ án tốt nghiệp của mình với đề tài: LexiVocab - Nền tảng học từ vựng chủ động đa môi trường."

**Ghi chú:**
- Giữ giọng điệu tự tin, nhìn thẳng vào hội đồng
- Không đọc từng chữ trên slide, chỉ giới thiệu tổng quan

---

### SLIDE 2: Mục tiêu đồ án
**Nội dung slide:**
- Xây dựng hệ sinh thái học từ vựng tiếng Anh đa nền tảng
- Ứng dụng thuật toán Spaced Repetition (SuperMemo-2)
- Tích hợp AI sinh tạo cho ngữ cảnh học tập thông minh
- Triển khai kiến trúc microservices với Docker & Terraform
- Đạt chuẩn production-ready

**Lời thoại:**
"Mục tiêu của đồ án là xây dựng một hệ sinh thái học từ vựng tiếng Anh đa nền tảng, ứng dụng thuật toán Spaced Repetition kết hợp AI sinh tạo. Hệ thống được thiết kế theo kiến trúc microservices, triển khai trên cloud với Docker và Terraform, đạt chuẩn production-ready."

---

### SLIDE 3: Bối cảnh & Động lực
**Nội dung slide:**
- Thống kê: 1.5 tỷ người đang học tiếng Anh toàn cầu
- 80% người học gặp khó khăn với từ vựng (theo khảo sát British Council)
- Thị trường EdTech toàn cầu đạt 404 tỷ USD năm 2025
- Các app hiện tại (Anki, Quizlet) thiếu tính liền mạch giữa các thiết bị

**Lời thoại:**
"Bối cảnh hiện nay, có khoảng 1.5 tỷ người đang học tiếng Anh toàn cầu, và 80% trong số đó gặp khó khăn lớn nhất với từ vựng. Thị trường EdTech đã đạt 404 tỷ USD. Tuy nhiên, các giải pháp hiện tại như Anki hay Quizlet vẫn thiếu tính liền mạch giữa các thiết bị, tạo ra khoảng trống mà LexiVocab hướng đến."

---

### SLIDE 4: Rào cản 1 - Context Switching
**Nội dung slide:**
- Minh họa: Người dùng đang đọc báo → gặp từ khó → Alt+Tab sang tab từ điển → quên mất ngữ cảnh
- Thống kê: Mất 23 phút để lấy lại focus sau mỗi lần gián đoạn (theo Gloria Mark, UC Irvine)
- Hiệu suất giảm 40% do chuyển đổi ngữ cảnh liên tục

**Lời thoại:**
"Rào cản thứ nhất là Context Switching. Khi đang đọc báo tiếng Anh, gặp từ khó, người học phải chuyển sang tab từ điển. Theo nghiên cứu của Gloria Mark tại UC Irvine, mỗi lần gián đoạn như vậy mất trung bình 23 phút để lấy lại focus. Hiệu suất học tập giảm tới 40%."

---

### SLIDE 5: Rào cản 2 - Đường cong quên lãng Ebbinghaus
**Nội dung slide:**
- Biểu đồ Ebbinghaus Forgetting Curve
- 80% kiến thức mới bị xóa sau 24h
- 60% sau 1 giờ, 70% sau 9 giờ, 80% sau 24h, 90% sau 1 tuần
- Vòng lặp "học xong để đó" - không có cơ chế ôn tập tự động

**Lời thoại:**
"Rào cản thứ hai là đường cong quên lãng Ebbinghaus. Không có ôn tập, 60% kiến thức bay mất sau 1 giờ, 80% sau 24h, và gần như toàn bộ sau 1 tuần. Đây là lý do tại sao nhiều người học từ vựng theo kiểu 'học xong để đó' mãi không tiến bộ."

---

### SLIDE 6: Tuyên ngôn giải pháp - 3 trụ cột
**Nội dung slide:**
- **Capture (Bắt từ):** Chrome Extension bắt từ vựng trực tiếp trên web, không cần chuyển tab
- **Remember (Nhớ lâu):** Thuật toán SuperMemo-2 tính toán chu kỳ ôn tập khoa học
- **Understand (Hiểu sâu):** AI sinh tạo ngữ cảnh thông minh, ví dụ thực tế

**Lời thoại:**
"LexiVocab giải quyết triệt để 2 rào cản này qua 3 trụ cột: Capture - bắt từ ngay trên web không cần chuyển tab; Remember - thuật toán SuperMemo-2 tính toán chu kỳ ôn tập khoa học; và Understand - AI sinh tạo ngữ cảnh thông minh giúp hiểu sâu thay vì tra cứu nhàm chán."

---

## PHẦN 2: HỆ SINH THÁI 4 MẢNH GHÉP (3 PHÚT)

### SLIDE 7: Tổng quan kiến trúc Hub & Spoke
**Nội dung slide:**
- Sơ đồ: Core API ở trung tâm, 3 client xung quanh
- Core API: .NET 10, chạy trên Azure
- Chrome Extension: Manifest V3, Shadow DOM
- Mobile App: React Native, Headless JS Widget
- Web Dashboard: NextJS, Analytics, Payment
- Nguyên lý: "Bao quát 100% không gian thiết bị"

**Lời thoại:**
"Hệ sinh thái LexiVocab được thiết kế theo kiến trúc Hub & Spoke. Core API viết bằng .NET 10 là trái tim hệ thống, kết nối với 3 client: Chrome Extension để bắt từ trên web, Mobile App React Native để ôn bài mọi lúc mọi nơi, và Web Dashboard NextJS để quản lý và thanh toán. Nguyên lý là bao quát 100% không gian thiết bị của người dùng."

---

### SLIDE 8: Chrome Extension - Chi tiết kỹ thuật
**Nội dung slide:**
- **Manifest V3:** Tuân thủ chuẩn mới nhất của Chrome, hiệu suất cao hơn V2
- **Shadow DOM:** Cô lập tuyệt đối mã CSS, popup không bị vỡ layout
- **Service Worker:** Xử lý background sync, không chiếm RAM
- **Content Script Injection:** Chèn script động vào DOM của trang web
- **Cross-origin API calls:** Gọi API từ extension qua CORS

**Lời thoại:**
"Chrome Extension sử dụng Manifest V3 - chuẩn mới nhất của Chrome. Điểm mạnh là Shadow DOM - UI của extension được nhốt trong 'lồng kính', CSS của website bên ngoài không thể xâm nhập. Service Worker xử lý background sync hiệu quả mà không chiếm RAM. Khi người dùng bôi đen từ vựng, Content Script chèn popup động vào DOM, gọi API qua CORS để lấy định nghĩa và lưu vào hệ thống."

---

### SLIDE 9: Mobile App - React Native & Widget
**Nội dung slide:**
- **React Native + Expo:** Codebase duy nhất cho iOS và Android
- **Headless JS Widget:** Chạy background task trên Android, hiển thị card từ vựng trên màn hình chờ
- **SecureStore:** Lưu token trong Keystore/Keychain native, chống truy cập trái phép
- **Flashcard Interactions:** Vuốt trái/phải đánh giá độ khó, animations Reanimated 3
- **Real-time Sync:** Đồng bộ với cloud qua WebSocket/SSE

**Lời thoại:**
"Mobile App được xây dựng bằng React Native với Expo, một codebase chạy được cả iOS và Android. Điểm nhấn là Widget Android sử dụng Headless JS - người dùng vuốt xem card từ màn hình chờ mà không cần mở app, tiết kiệm pin. SecureStore lưu token trong Keystore/Keychain native của thiết bị. Flashcard có animations mượt mà bằng Reanimated 3, đồng bộ real-time với cloud."

---

### SLIDE 10: Web Dashboard - NextJS
**Nội dung slide:**
- **NextJS 16:** App Router, Server Components, tối ưu SEO
- **Analytics & Reports:** Biểu đồ tiến độ học tập (Recharts/D3.js)
  - Số lượng từ đã học
  - Tỷ lệ ghi nhớ theo thời gian
  - Streak days (ngày học liên tiếp)
  - Heatmap học tập
- **Payment Integration:** SePay QR, tự động kích hoạt VIP qua webhook
- **Management Console:** Quản lý từ vựng, cài đặt thông báo, xuất báo cáo PDF
- **i18n:** Hỗ trợ 4 ngôn ngữ (Việt, Anh, Nhật, Hàn)

**Lời thoại:**
"Web Dashboard xây dựng trên NextJS 14 với App Router và Server Components, tối ưu SEO. Dashboard hiển thị biểu đồ tiến độ học tập chi tiết: số từ đã học, tỷ lệ ghi nhớ, streak days, và heatmap học tập. Tích hợp SePay để thanh toán QR, tự động kích hoạt gói VIP qua webhook. Hệ thống hỗ trợ 4 ngôn ngữ, đổi phát là thay đổi ngay lập tức."

---

## PHẦN 3: KIẾN TRÚC BACKEND & BẢO MẬT (4 PHÚT)

### SLIDE 11: Clean Architecture - Tổng quan
**Nội dung slide:**
- Sơ đồ 4 lớp:
  - **Presentation Layer:** Controllers, DTOs, Middleware
  - **Application Layer:** Use Cases, Commands, Queries, DTOs
  - **Domain Layer (Core):** Entities, Value Objects, Domain Services, Interfaces
  - **Infrastructure Layer:** Database, External APIs, Caching, Message Queue
- Dependency Rule: Dependencies chỉ hướng vào trong
- Domain Layer không phụ thuộc framework hay database

**Lời thoại:**
"Backend áp dụng Clean Architecture với 4 lớp rõ ràng. Quy tắc quan trọng nhất là Dependency Rule - các dependency chỉ được hướng vào trong. Domain Layer ở trung tâm chứa business logic thuần túy, không phụ thuộc vào bất kỳ framework hay database nào. Điều này có nghĩa là nếu em muốn đổi từ PostgreSQL sang MongoDB, không cần sửa một dòng code trong Domain Layer."

---

### SLIDE 12: CQRS - Phân tách Command & Query
**Nội dung slide:**
- **Command:** Thao tác Ghi (Create, Update, Delete)
  - CreateFlashcardCommand
  - UpdateReviewResultCommand
  - ProcessPaymentCommand
- **Query:** Thao tác Đọc (Read)
  - GetDueFlashcardsQuery
  - GetUserStatisticsQuery
  - GetPaymentHistoryQuery
- **Lợi ích:**
  - Tối ưu hóa riêng cho từng loại thao tác
  - Dễ scale: Có thể scale Query side độc lập với Command side
  - Giảm contention trên database

**Lời thoại:**
"Kết hợp với Clean Architecture là CQRS - Command Query Responsibility Segregation. Mọi thao tác được chia rõ ràng: Command cho Ghi, Query cho Đọc. Ví dụ, CreateFlashcardCommand xử lý việc tạo thẻ mới, còn GetDueFlashcardsQuery lấy danh sách thẻ cần ôn. Lợi ích là có thể tối ưu và scale riêng từng bên. Query side có thể dùng read replica database, trong khi Command side ghi vào primary database."

---

### SLIDE 13: MediatR & Pipeline Behaviors
**Nội dung slide:**
- **MediatR:** Thư viện implement CQRS trong .NET
- **Request/Handler pattern:** Mỗi Command/Query có một Handler riêng
- **Pipeline Behaviors:**
  - Validation Behavior: Validate input trước khi vào Handler
  - Logging Behavior: Ghi log tự động
  - Caching Behavior: Cache kết quả Query
  - Transaction Behavior: Wrap Command trong transaction
- **Cross-cutting concerns** được xử lý tập trung

**Lời thoại:**
"Em sử dụng MediatR để implement CQRS trong .NET. Pattern là Request/Handler - mỗi Command hay Query có một Handler riêng biệt. Điểm mạnh là Pipeline Behaviors - em có thể chèn các cross-cutting concern như validation, logging, caching, và transaction management vào pipeline một cách tập trung, không cần sửa từng Handler."

---

### SLIDE 14: Cơ chế xác thực kép - Stateful Identity
**Nội dung slide:**
- **Access Token:**
  - Thời hạn: 15 phút
  - Lưu trong RAM (memory)
  - Xác thực mọi request API
  - JWT với RS256 (asymmetric signing)
- **Refresh Token:**
  - Thời hạn: 7 ngày
  - Lưu trong HttpOnly Cookie
  - Không thể truy cập bởi JavaScript
  - Chống tấn công XSS
- **Flow:** Access hết hạn → Gọi refresh endpoint → Verify Refresh Token → Cấp Access Token mới

**Lời thoại:**
"Về bảo mật, em triển khai cơ chế xác thực kép Stateful Identity. Access Token có thời hạn 15 phút, lưu trong RAM, dùng JWT với thuật toán RS256. Refresh Token có thời hạn 7 ngày, được ghim vào trình duyệt dạng HttpOnly Cookie - JavaScript không thể đọc được, chống hoàn toàn tấn công XSS. Khi Access hết hạn, hệ thống tự động gọi refresh endpoint để cấp token mới, user không cần đăng nhập lại."

---

### SLIDE 16: Database Design & Entity Framework
**Nội dung slide:**
- **PostgreSQL:** Database chính, hỗ trợ JSONB và Full-text search
- **Entity Framework Core 9:** ORM với Code First Migration
- **Key entities:**
  - User, Vocabulary, UserVocabulary, ReviewLog, Payment, Subscription
- **Relationships:**
  - User 1-N Flashcard
  - Flashcard 1-N ReviewLog
  - User 1-N Payment
- **Soft Delete:** Không xóa hard, chỉ đánh dấu IsDeleted

**Lời thoại:**
"Database sử dụng PostgreSQL với Entity Framework Core 9 theo pattern Code First. Các entity chính bao gồm User, Vocabulary, UserVocabulary, ReviewLog, Payment, Subscription
. Mối quan hệ được thiết kế chuẩn hóa: một User có nhiều Vocabulary, ReviewLog để theo dõi lịch sử ôn tập. Em áp dụng Soft Delete để bảo toàn dữ liệu, không xóa hard mà chỉ đánh dấu IsArchieved."

---

## PHẦN 4: USE-CASES LÕI & XỬ LÝ TẢI (4 PHÚT)

### SLIDE 17: Thuật toán SuperMemo-2
**Nội dung slide:**
- **Công thức chính:**
  - EF (Easiness Factor) = EF + (0.1 - (5-q) * (0.08 + (5-q) * 0.02))
  - I(n) = I(n-1) * EF (interval giữa các lần ôn)
- **Rating scale:** 0 (Hard) → 5 (Easy)
- **Kết quả:**
  - Độ khó 0-2: 1-3 ngày
  - Độ khó 3-5: 1-4 tháng
- **Fuzz Factor:** Cộng trừ ngẫu nhiên 0-20% interval để tránh dồn thẻ

**Lời thoại:**
"Điểm sáng nhất của app là thuật toán Spaced Repetition SuperMemo-2. Khi người dùng vuốt thẻ flashcard và đánh giá độ khó từ 0 đến 5, hệ thống tính toán EF - Easiness Factor, và từ đó ra interval kế tiếp. Đánh giá khó thì 1-3 ngày sau ôn lại, đánh giá dễ thì 1-4 tháng sau. Em còn thêm Fuzz Factor - cộng trừ ngẫu nhiên vài phần trăm interval để tránh hiện tượng tháng sau hàng loạt thẻ dồn lại cùng một ngày."

---

### SLIDE 18: B-Tree Indexing & Query Optimization
**Nội dung slide:**
- **Vấn đề:** Query "lấy thẻ cần ôn hôm nay" phải full-scan bảng Flashcard
- **Giải pháp:** B-Tree Index trên cột NextReviewDate
- **Kết quả:**
  - Full-Scan: ~500ms cho 10K user
  - Index Scan: < 1 micro-giây
  - Giảm 99.9% thời gian truy vấn
- **Composite Index:** (UserId, NextReviewDate) cho query có điều kiện user

**Lời thoại:**
"Về tối ưu database, query quan trọng nhất là 'lấy thẻ cần ôn hôm nay'. Ban đầu phải full-scan toàn bộ bảng Flashcard, mất ~500ms với 10K user. Em tạo B-Tree Index trên cột NextReviewDate, kết hợp với Composite Index (UserId, NextReviewDate). Kết quả là thời gian truy vấn giảm xuống dưới 1 micro-giây, giảm 99.9% so với trước."

---

### SLIDE 19: AI Streaming với Server-Sent Events (SSE)
**Nội dung slide:**
- **Vấn đề:** AI response mất 3-5 giây, user phải chờ
- **Giải pháp:** SSE - Server-Sent Events
  - Kết nối HTTP persistent, server push data về client
  - FlushAsync liên tục sau mỗi token
  - Trải nghiệm type-writer zero-delay
- **So sánh với WebSocket:**
  - SSE: 1 chiều (server→client), nhẹ, dùng HTTP
  - WebSocket: 2 chiều, nặng hơn, cần handshake riêng
  - SSE phù hợp hơn cho streaming text từ AI
- **Flow:** User request → API gọi LLM → Stream token về → FlushAsync → Client nhận từng từ

**Lời thoại:**
"Thứ hai là AI Streaming. Thay vì bắt user chờ 5 giây để AI trả về toàn bộ response, em dùng Server-Sent Events. Đây là kết nối HTTP persistent, server push từng token về client ngay khi có. Client nhận được từng từ một, tạo hiệu ứng type-writer. SSE khác WebSocket ở chỉ là 1 chiều và dùng HTTP thuần, nhẹ hơn nhiều, rất phù hợp cho use case streaming text từ AI."

---

### SLIDE 20: Tích hợp AI - LLM & Prompt Engineering
**Nội dung slide:**
- **Model:** OpenAI GPT-4 / Claude / Gemini (configurable)
- **Use cases:**
  - Giải nghĩa từ vựng với ngữ cảnh
  - Tạo câu chuyện ngắn giúp nhớ từ
  - Phân tích ngữ pháp
  - Dịch thuật thông minh
- **Prompt Engineering:**
  - System prompt cố định định hình style
  - Few-shot prompting với ví dụ mẫu
  - Temperature 0.7 để cân bằng creativity và accuracy
- **Cost optimization:** Cache kết quả phổ biến, batch requests

**Lời thoại:**
"Hệ thống tích hợp LLM từ OpenAI, Claude, hoặc Gemini - configurable. Các use case bao gồm giải nghĩa với ngữ cảnh, tạo câu chuyện ngắn giúp nhớ từ, phân tích ngữ pháp, và dịch thuật. Em áp dụng Prompt Engineering với system prompt cố định để định hình style, few-shot prompting với ví dụ mẫu, và temperature 0.7 để cân bằng. Để tối ưu chi phí, em cache kết quả cho các từ phổ biến và batch requests khi có thể."

---

### SLIDE 21: Thanh toán tự động - SePay & Idempotency
**Nội dung slide:**
- **SePay Integration:**
  - Tạo QR code thanh toán
  - Webhook callback khi thanh toán thành công
  - Tự động kích hoạt gói VIP
- **Vấn đề:** Webhook có thể gửi trùng lặp do network timeout
- **Giải pháp Idempotency:**
  - Cột TransactionId đánh UNIQUE KEY
  - 5 request trùng → 1 kết quả duy nhất
  - DB báo lỗi 23505 (PostgreSQL unique violation) từ lần 2
  - Không bao giờ cộng VIP 2 lần
- **State Machine:** Pending → Processing → Completed / Failed

**Lời thoại:**
"Về thanh toán, em tích hợp SePay - cổng thanh toán QR phổ biến tại Việt Nam. Khi user quét QR và thanh toán, SePay gửi webhook về hệ thống. Tuy nhiên, do network timeout, webhook có thể gửi trùng lặp. Em xử lý bằng Idempotency: cột TransactionId được đánh UNIQUE KEY. Dù SePay gửi 10 lần, từ lần thứ 2 database sẽ báo lỗi unique violation và bỏ qua, đảm bảo tiền vào 1 lần thì VIP chỉ được cộng 1 lần. Em cũng áp dụng State Machine để theo dõi trạng thái giao dịch."

---

### SLIDE 22: Background Jobs với Hangfire
**Nội dung slide:**
- **Hangfire:** Thư viện background job cho .NET
- **Các job chính:**
  - SendReviewReminder: Gửi push notification nhắc ôn bài
  - ProcessExpiredSubscriptions: Kiểm tra và hạ cấp VIP hết hạn
  - GenerateDailyReport: Tổng hợp báo cáo học tập hàng ngày
  - CleanupOldLogs: Xóa log cũ sau 90 ngày
- **Scheduling:** Cron expression cho job định kỳ
- **Dashboard:** UI quản lý job, retry failed jobs

**Lời thoại:**
"Hệ thống sử dụng Hangfire để xử lý background jobs. Các job chính bao gồm: gửi push notification nhắc ôn bài, kiểm tra và hạ cấp VIP hết hạn, tổng hợp báo cáo hàng ngày, và dọn dẹp log cũ. Hangfire hỗ trợ scheduling bằng Cron expression và có dashboard để quản lý job, retry các job thất bại."

---

## PHẦN 5: TRIỂN KHAI & DEVOPS (2 PHÚT)
---

### SLIDE 25: Docker Containerization
**Nội dung slide:**
Multi-stage Dockerfile:

 - Stage 1 (Build): Sử dụng .NET SDK để biên dịch code.

 - Stage 2 (Runtime): Chỉ dùng ASP.NET Runtime để chạy app.

 - Tối ưu: Giảm dung lượng Image từ ~1GB xuống còn 200MB, tăng tốc độ deploy.

Hệ sinh thái Railway:

- Auto-provisioning: Tự động khởi tạo PostgreSQL và Redis Cloud.

- CI/CD Pipeline: Tự động build và deploy ngay khi push code lên GitHub.

- Environment Management: Quản lý tập trung Connection Strings và API Keys.

Khả năng vận hành:

- Health checks: Theo dõi trạng thái container.

- Automatic SSL: Tự động cấp phát chứng chỉ HTTPS cho domain.

**Lời thoại:**
"Về phần triển khai, em sử dụng Docker với kỹ thuật Multi-stage build. Cụ thể, em dùng một image SDK đầy đủ để build source code, sau đó chỉ copy file thực thi sang một image Runtime siêu nhẹ. Việc này giúp giảm dung lượng image từ gần 1GB xuống còn khoảng 200MB, giúp việc triển khai nhanh và bảo mật hơn.

Thay vì chạy server vật lý, em chọn nền tảng PaaS Railway để deploy. Khi em push code lên GitHub, Railway sẽ tự động trigger quy trình CI/CD để build Docker image và cập nhật phiên bản mới nhất. Các dịch vụ như PostgreSQL và Redis cũng được cấu hình trực tiếp trên môi trường Cloud của Railway, giúp hệ thống hoạt động ổn định, có sẵn HTTPS và cơ chế tự động restart nếu container gặp sự cố (Health checks)."

---

## PHẦN 6: DEMO THỰC CHIẾN (10 PHÚT)

> **Chuẩn bị trước:**
> - Mở sẵn Chrome với Extension đã cài
> - Mở sẵn Android Emulator với app đã đăng nhập
> - Mở sẵn Web Dashboard
> - Tài khoản demo đã có data đẹp (biểu đồ, streak, từ vựng)

---

### DEMO 1: Chrome Extension - Bắt từ vựng (2.5 phút)

**Thao tác:**
1. Mở trang báo tiếng Anh (CNN, BBC, hoặc bất kỳ)
2. Bôi đen 1 từ khó (ví dụ: "serendipity")
3. Popup tự động hiện định nghĩa, phát âm, và ví dụ
4. Bấm "Save" → Từ lưu vào hệ thống
5. Bôi đen 1 từ khác để demo lại

**Lời thoại:**
"Em demo tính năng đầu tiên - Chrome Extension. Đang đọc báo tiếng Anh, gặp từ khó, em chỉ cần bôi đen. Popup tự động hiện định nghĩa, phát âm, và ví dụ. Bấm Save, từ này lập tức bay lên Cloud và đồng bộ sang Mobile App. Không cần Alt+Tab, không mất focus."

**Điểm nhấn kỹ thuật cần nói:**
- Shadow DOM đảm bảo popup không bị vỡ layout
- Service Worker xử lý background sync
- API call qua CORS với Access Token

---

### DEMO 2: Mobile App - Ôn tập Flashcard (2.5 phút)

**Thao tác:**
1. Mở Android Emulator
2. Vào app LexiVocab
3. Bấm "Start Review" → Hiện danh sách thẻ cần ôn hôm nay
4. Lật thẻ → Chọn mức độ (Hard/Easy)
5. Demo vuốt 2-3 thẻ
6. Hiển thị kết quả sau session

**Lời thoại:**
"Sang Mobile App, hệ thống tự biết hôm nay có từ nào cần ôn dựa trên thuật toán SuperMemo-2. Em lật thẻ, đọc từ, và chọn mức độ. Nếu dễ, thuật toán sẽ đẩy từ này ra 1 tháng sau. Nếu khó, 1 ngày sau sẽ gặp lại. Giao diện có haptic feedback - rung nhẹ khi vuốt, tạo trải nghiệm mượt mà."

**Điểm nhấn kỹ thuật cần nói:**
- SuperMemo-2 tính toán interval real-time
- Reanimated 3 cho animations
- SecureStore cho token
- Real-time sync với cloud

---

### DEMO 3: AI Giải thích Real-time (2.5 phút)

**Thao tác:**
1. Chọn 1 từ khó trong app
2. Bấm "Explain with AI"
3. Demo hiệu ứng streaming - chữ chạy ra từng từ
4. Bấm "Generate Story" → AI tạo câu chuyện ngắn

**Lời thoại:**
"Khi gặp từ quá khó hiểu, bấm nút Explain with AI. Thay vì chờ 5 giây, AI stream text về từng từ một qua SSE - Server-Sent Events. Trải nghiệm giống ChatGPT, zero delay. Hoặc bấm Generate Story, AI sẽ tạo một câu chuyện ngắn có chứa từ này, giúp nhớ lâu hơn qua ngữ cảnh."

**Điểm nhấn kỹ thuật cần nói:**
- SSE vs WebSocket: 1 chiều, nhẹ hơn
- FlushAsync liên tục
- Prompt engineering cho storytelling

---

### DEMO 4: Widget & Thống kê đa ngôn ngữ (2.5 phút)

**Thao tác:**
1. Ra ngoài màn hình Home Android
2. Chỉ vào Widget hiển thị từ vựng
3. Vuốt Widget để xem từ tiếp theo
4. Vào Web Dashboard → Hiển thị biểu đồ tiến độ
5. Vào Cài đặt → Đổi ngôn ngữ (Việt → Nhật)
6. Quay lại Dashboard → UI đã đổi ngôn ngữ

**Lời thoại:**
"App còn có Widget ngoài màn hình Home - user thấy từ vựng mỗi khi mở điện thoại mà không cần mở app. Sang Web Dashboard, đây là biểu đồ tiến độ học tập: số từ đã học, tỷ lệ ghi nhớ, streak days. Đặc biệt, hệ thống hỗ trợ 4 ngôn ngữ. Em đổi sang tiếng Nhật, toàn bộ UI thay đổi ngay lập tức - từ app, extension, đến dashboard."

**Điểm nhấn kỹ thuật cần nói:**
- Headless JS cho Widget Android
- Recharts/D3.js cho biểu đồ
- i18n với react-intl / next-intl

---

## PHẦN 7: KẾT LUẬN & HỎI ĐÁP (2 PHÚT)

### SLIDE 26: Tổng kết đóng góp
**Nội dung slide:**
- **Kiến trúc:** Clean Architecture + CQRS, dễ bảo trì và scale
- **Bảo mật:** Token kép, Rate Limiting, 8 lớp Security Headers
- **Thuật toán:** SuperMemo-2 + Fuzz Factor, tối ưu trải nghiệm học
- **AI:** SSE Streaming, Prompt Engineering, Cost Optimization
- **DevOps:** Docker, Terraform, CI/CD, Production-ready
- **Đa nền tảng:** Chrome Extension, Mobile, Web - 100% coverage

**Lời thoại:**
"Tóm lại, LexiVocab không chỉ là đồ án, mà là một sản phẩm Production-Ready. Em đã xây dựng kiến trúc Clean Architecture kết hợp CQRS, triển khai cơ chế bảo mật đa lớp, tối ưu thuật toán SuperMemo-2, tích hợp AI Streaming qua SSE, và thiết lập pipeline CI/CD hoàn chỉnh. Hệ sinh thái bao phủ 100% không gian thiết bị từ Chrome Extension, Mobile App, đến Web Dashboard."

---

### SLIDE 27: Hạn chế & Hướng phát triển
**Nội dung slide:**
- **Hạn chế hiện tại:**
  - Chưa hỗ trợ offline mode hoàn toàn
  - Widget chỉ có trên Android, chưa có iOS
  - AI cost còn cao với số lượng user lớn
- **Hướng phát triển:**
  - Service Worker + IndexedDB cho offline mode
  - iOS Widget với SwiftUI
  - Fine-tuned model nhỏ hơn (Llama 3B) để giảm cost
  - Social features: Học nhóm, Leaderboard
  - SRS cho ngôn ngữ khác (Nhật, Hàn, Trung)

**Lời thoại:**
"Về hạn chế, hiện tại hệ thống chưa hỗ trợ offline mode hoàn toàn, Widget chỉ có trên Android, và chi phí AI còn cao khi scale. Hướng phát triển tương lai bao gồm offline mode với Service Worker và IndexedDB, iOS Widget, fine-tuned model nhỏ hơn để giảm cost, và mở rộng sang các ngôn ngữ khác."

---

### SLIDE 28: Cảm ơn & Mời hỏi đáp
**Nội dung slide:**
- Cảm ơn Hội đồng đã lắng nghe
- Thông tin liên hệ
- Mời đặt câu hỏi

**Lời thoại:**
"Phần trình bày của em xin hết. Em xin chân thành cảm ơn quý thầy cô đã lắng nghe. Mọi góp ý của quý thầy cô là bài học quý giá cho hành trình kỹ sư tương lai của em. Kính mời quý thầy cô đặt câu hỏi ạ."

---

## 🧠 BÍ KÍP TRẢ LỜI CÂU HỎI PHẢN BIỆN

### Câu hỏi 1: Tại sao dùng Clean Architecture?
**Trả lời:**
"Clean Architecture tách biệt logic cốt lõi ra khỏi framework và database. Khi cần đổi database từ Postgres sang MongoDB, em không cần sửa một dòng code trong Domain Layer. Ngoài ra, việc viết unit test trở nên dễ dàng vì business logic không phụ thuộc vào external dependencies."

### Câu hỏi 2: SM-2 của em có gì hay hơn Anki?
**Trả lời:**
"Em thêm Fuzz Factor - cộng trừ ngẫu nhiên 0-20% interval. Nếu không có Fuzz Factor, khi user học nhiều từ cùng ngày, tháng sau hàng loạt thẻ sẽ dồn lại cùng một ngày, tạo ra 'review hell'. Fuzz Factor phân tán thẻ ra các ngày khác nhau, trải nghiệm học mượt mà hơn."

### Câu hỏi 3: SSE khác gì WebSocket? Tại sao chọn SSE?
**Trả lời:**
"WebSocket là 2 chiều, cần handshake riêng, và giữ kết nối persistent. SSE là 1 chiều - chỉ server push về client, dùng HTTP thuần, nhẹ hơn nhiều. Với use case streaming text từ AI, chỉ cần server push về client, không cần client gửi ngược. SSE là lựa chọn tối ưu về performance và đơn giản."

### Câu hỏi 4: Lỡ Webhook thanh toán bị lỗi gửi 2 lần thì sao?
**Trả lời:**
"Em cài đặt cột TransactionId là UNIQUE KEY trong database. Dù SePay gửi 10 lần, từ lần thứ 2 PostgreSQL sẽ báo lỗi 23505 - unique violation, và hệ thống bỏ qua. User không bao giờ được cộng VIP 2 lần. Đây là kỹ thuật Idempotency - 5 request trùng cho 1 kết quả duy nhất."

### Câu hỏi 5: Extension làm sao để không bị vỡ giao diện trên các web khác nhau?
**Trả lời:**
"Em dùng Shadow DOM - một công nghệ của Web Components. UI của extension được nhốt trong một 'lồng kính' riêng biệt. CSS của website bên ngoài không thể xâm nhập vào phá hỏng giao diện extension. Điều này đảm bảo popup hiển thị nhất quán trên mọi trang web, từ CNN đến Reddit."

### Câu hỏi 6: Tại sao chọn .NET mà không phải Node.js?
**Trả lời:**
".NET 10 có performance cao với Native AOT compilation, type safety mạnh với C#, và hệ sinh thái enterprise-ready. Với use case cần tính toán thuật toán SM-2 và xử lý concurrency cao, .NET cho throughput tốt hơn. Ngoài ra, MediatR và EF Core là những thư viện rất mạnh cho Clean Architecture."

### Câu hỏi 7: Làm sao đảm bảo sync real-time giữa các thiết bị?
**Trả lời:**
"Khi user bắt từ qua Extension, API ghi vào database và đồng thời publish event lên Redis Pub/Sub. Mobile App subscribe channel này qua WebSocket/SSE, nhận notification và refresh data. Ngoài ra, app cũng polling mỗi khi mở lên để đảm bảo data mới nhất."

### Câu hỏi 8: Rate Limiting có ảnh hưởng đến user hợp lệ không?
**Trả lời:**
"Em thiết kế 3 tier rate limiting: Tier 1 cho API thường (100 req/phút), Tier 2 cho API thanh toán (10 req/phút), và Tier 3 cho AI streaming (1000 req/giờ). Các giới hạn này rất cao so với usage thông thường. User hợp lệ không bao giờ đụng trần, chỉ bot hoặc attacker mới bị chặn."

### Câu hỏi 9: Chi phí AI cao, em tối ưu như thế nào?
**Trả lời:**
"Em áp dụng 3 chiến lược: Cache kết quả cho các từ phổ biến - nếu 100 user hỏi từ 'serendipity', chỉ gọi API 1 lần. Batch requests khi có thể. Và prompt engineering để giảm số token output. Trong tương lai, em plan fine-tune model nhỏ hơn như Llama 3B chạy local để giảm cost xuống gần như bằng 0."

### Câu hỏi 10: Tại sao chọn SePay mà không phải Stripe/PayPal?
**Trả lời:**
"SePay là cổng thanh toán QR phổ biến tại Việt Nam, hỗ trợ chuyển khoản ngân hàng nội địa, phí thấp hơn Stripe. Với target user là người Việt, SePay mang lại trải nghiệm thanh toán quen thuộc - quét QR bằng app ngân hàng. Tuy nhiên, architecture của em được thiết kế modular, dễ dàng thêm Stripe hay PayPal sau này nếu mở rộng quốc tế."

---

## 📋 CHECKLIST TRƯỚC KHI BÁO CÁO

### Chuẩn bị môi trường
- [ ] Slide đã chạy thử trên máy chiếu (font, màu sắc, animation)
- [ ] Chrome Extension đã cài và test trên 2-3 trang web khác nhau
- [ ] Android Emulator đã mở sẵn, app đã đăng nhập
- [ ] Web Dashboard đã mở sẵn trên tab khác
- [ ] Tài khoản demo có data đẹp (biểu đồ, streak > 7 ngày, > 50 từ)
- [ ] Kết nối mạng ổn định (hoặc có backup 4G)
- [ ] Sạc pin laptop đầy, mang theo sạc dự phòng

### Chuẩn bị nội dung
- [ ] Đã đọc thuộc phần mở đầu và kết luận
- [ ] Đã luyện demo 3 lần, đo thời gian
- [ ] Đã chuẩn bị câu trả lời cho 10 câu hỏi phản biện phổ biến
- [ ] Đã in 3 bản tóm tắt đề tài cho hội đồng (optional)

### Trang phục & thái độ
- [ ] Trang phục lịch sự (áo sơ mi, quần tây)
- [ ] Giọng nói to, rõ ràng, tự tin
- [ ] Nhìn thẳng vào hội đồng, không cúi mặt xuống laptop
- [ ] Cảm ơn sau mỗi câu hỏi, dù câu hỏi có khó

---

## 🎯 MẸO THUYẾT TRÌNH

1. **Nhịp độ:** Không nói quá nhanh. 15 phút cho 12 slide nghĩa là ~75 giây/slide.
2. **Tương tác:** Chỉ tay vào slide khi nói đến sơ đồ, biểu đồ.
3. **Pause:** Dừng 1-2 giây sau câu quan trọng để hội đồng tiêu hóa.
4. **Demo:** Luôn có plan B - nếu mạng lag, show screenshot/video quay sẵn.
5. **Hỏi đáp:** Nếu không biết câu trả lời, thành thật: "Em chưa nghĩ đến khía cạnh này, em sẽ tìm hiểu thêm ạ."
"""

# Lưu file
with open('/mnt/agents/output/KichBan_BaoCao_LexiVocab_ChiTiet.md', 'w', encoding='utf-8') as f:
    f.write(script_content)

print("✅ Đã tạo kịch bản báo cáo chi tiết!")
print(f"📄 File: KichBan_BaoCao_LexiVocab_ChiTiet.md")
print(f"📊 Tổng số slide: 28")
